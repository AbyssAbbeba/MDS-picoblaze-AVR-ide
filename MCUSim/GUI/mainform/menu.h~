#ifndef MENU_H
#define MENU_H

#include <QMainWindow>
#include <QMenu>



class MenuMan
{
    public:
        MenuMan(QMainWindow *mainWindow);
        //vytvareni pomoci konfiguracniho souboru
        MenuMan(QMainWindow *, QString path);
        void loadDefault();
        void addMenu(QString name);
        void addAction(QString menuName, QAction *action);

        QVector<QMenu*> Menus;

    private:
        QMainWindow *mainWindowPtr;
};



/*class Menu: public QMenu
{
    public:
        Menu();


    private:
        QMenu *fileMenu;
        QMenu *editMenu;
        QMenu *viewMenu;
        QMenu *projectMenu;
        QMenu *toolsMenu;
        QMenu *helpMenu;

};*/


#endif
